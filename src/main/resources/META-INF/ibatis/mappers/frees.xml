<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE sqlMap PUBLIC "-//iBATIS.com//DTD SQL Map 2.0//EN" 
	"http://ibatis.apache.org/dtd/sql-map-2.dtd">
 
<sqlMap namespace="frees">

	<select id="getFreeByNo" parameterClass="int" resultClass="com.community.vo.Free">
		select
			post_no as no,
			post_board_no as boardNo,
			post_title as title,
			post_writer_no as writerNo,
			post_content as content,
			post_important as important,
			post_read_count as readCount,
			post_suggestion_count as suggestionCount,
			post_comment_count as commentCount,
			post_deleted as deleted,
			post_created_date as createdDate,
			post_updated_date as updatedDate,
			post_deleted_date as deletedDate,
			post_original_no as originalNo
		from
			comm_posts
		where
			post_board_no = 102
			and post_no = #value#
	</select>
	
	<select id="getAllFree" resultClass="com.community.vo.Free">
		select
			post_no as no,
			post_board_no as boardNo,
			post_title as title,
			post_writer_no as writerNo,
			post_content as content,
			post_important as important,
			post_read_count as readCount,
			post_suggestion_count as suggestionCount,
			post_comment_count as commentCount,
			post_deleted as deleted,
			post_created_date as createdDate,
			post_updated_date as updatedDate,
			post_deleted_date as deletedDate,
			post_original_no as originalNo
		from
			comm_posts
		where
			post_board_no = 102
	</select>
	
	<update id="updateFree" parameterClass="com.community.vo.Free">
		update comm_posts
			(post_board_no, post_title, post_writer, post_content, post_important, post_read_count,
			post_suggestion_count, post_comment_count, post_updated_date, post_original_no)
		set
			(#boardNo#, #title#, #writerNo#, #content#, #important#, #readCount#, #suggestionCount#,#commentCount#,sysdate,#originalNo#)
		where
			post_no = #no#
	</update>
</sqlMap>

